package com.onmo.wgames.sdk;

/**
 * Created by Srini on 14/02/2018.
 *
 * Enum used to know if the call need to be only from cache, only from live or both
 */

public enum CacheCriteriaType {
	FROM_LIVE,
	FROM_CACHED;
}
